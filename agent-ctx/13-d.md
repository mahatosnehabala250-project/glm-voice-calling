# Task 13-d: Help Center, Notifications Panel, Command Palette Update

## Work Log
- Added `helpCenterOpen`, `setHelpCenterOpen`, `toggleHelpCenter` to `app-store.ts` Zustand store
- Created `/src/components/shared/help-center.tsx` — comprehensive help center dialog:
  - Large search input with emerald focus ring ("Search help articles...")
  - Quick Links Grid (2x2): Getting Started (Rocket/emerald), AI Configuration (Brain/teal), Billing & Plans (CreditCard/amber), API Documentation (FileCode/violet)
  - FAQ Accordion (5 items using shadcn/ui Accordion component):
    1. How does VoiceAI handle incoming calls?
    2. Can I customize the AI greeting?
    3. How are appointments confirmed?
    4. What happens when a patient asks for a human?
    5. How do I upgrade my plan?
  - Contact Support Card: email (support@voiceai.in), phone (+91-80-4567-8900), WhatsApp, "Schedule a Demo" CTA button
  - Search filters both FAQ items and quick links
  - Framer Motion animations on sections and items
  - Emerald/teal color scheme, responsive design
- Created `/src/components/shared/notifications-panel.tsx` — enhanced notifications panel:
  - Props: `isOpen: boolean`, `onClose: () => void`
  - Fixed positioned panel (right side, below header), 400px wide, scrollable
  - Header with "Notifications" title, count badge, "Mark all read" button
  - Filter tabs: All | Unread | Alerts (with count badges)
  - Notification items with left colored border (emerald=info/success, amber=alert, rose=error)
  - Icon based on type (Bell, AlertTriangle, CheckCircle, Info)
  - Title + description + relative time + unread dot indicator
  - Empty state when no notifications
  - "View All" link at bottom
  - Click-outside to close + Escape key to close
  - Framer Motion AnimatePresence for open/close
  - Uses shadcn/ui Badge, Button, ScrollArea, Card
- Updated `/src/components/shared/command-palette.tsx`:
  - Added `HelpCircle` icon import
  - Added `setHelpCenterOpen` to destructured store
  - Added "Open Help Center" command with `Ctrl+H` shortcut hint for both admin and client roles
  - Added `setHelpCenterOpen` to useMemo dependency array
- Wired HelpCenter into page.tsx (global render alongside CommandPalette)
- Updated header.tsx "Help & Support" button to open Help Center via `useAppStore.getState().setHelpCenterOpen(true)`
- Added `Ctrl+H` shortcut badge to Help & Support menu item

## Files Modified
1. `src/stores/app-store.ts` — Added helpCenterOpen state
2. `src/components/shared/help-center.tsx` — NEW file
3. `src/components/shared/notifications-panel.tsx` — NEW file
4. `src/components/shared/command-palette.tsx` — Added Help Center command
5. `src/app/page.tsx` — Imported and rendered HelpCenter
6. `src/components/shared/header.tsx` — Wired Help & Support button to Help Center

## Verification
- ESLint: 0 errors
- Dev server: compiles successfully (✓ Compiled)
- No existing component logic modified (only additive changes)
